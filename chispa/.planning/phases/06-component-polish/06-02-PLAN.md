# Plan 06-02: VerdictCard + MarketSnapshot + CompetitorCard

**Phase:** 6 — Component Polish
**Covers:** UI-09, UI-12, UI-11

## VerdictCard Changes

```tsx
// Verdict word: font-display (Space Grotesk), text-7xl, font-bold
<h2
  className="font-display text-7xl font-bold mb-4 tracking-tight"
  style={{ color: config.color }}
>
  {config.label}
</h2>

// Container: more padding, thicker border
<motion.div
  className="bg-surface border-2 rounded-xl p-8"
  style={{ backgroundColor: config.bg, borderColor: config.color + '60' }}
>
```

## MarketSnapshot Changes

```tsx
// Values: text-xl font-semibold instead of text-sm font-medium
<p className="text-white font-semibold text-xl leading-snug">{marketSize}</p>
<p className="text-white font-semibold text-xl leading-snug">{marketGrowth}</p>

// Labels: keep text-xs text-gray-500 uppercase tracking-wider
```

## CompetitorCard Changes

```tsx
// Favicon: 28x28px, rounded-md
<img
  src={...}
  alt=""
  width={28}
  height={28}
  className="rounded-md flex-shrink-0"
/>

// Name: text-base font-semibold
<h4 className="font-semibold text-white text-base leading-tight">{name}</h4>
```

## Acceptance Criteria

- [ ] Verdict word renders in Space Grotesk (visually distinct from Inter)
- [ ] Verdict text at approximately 72px — dominant on screen
- [ ] Border is 2px and color-matched (not 1px with faint color)
- [ ] Market size and growth values readable at 10 feet from screen
- [ ] Competitor favicon is 28px with rounded corners
- [ ] Competitor name visually larger than description text
- [ ] `npm run typecheck` passes
